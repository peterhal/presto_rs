#[allow(non_upper_case_globals, non_camel_case_types)]
enum TokenKind {
    // Common kinds
    BOF,
    EOF,
    ERROR,

    // operators and punctuators
    OpenParen,
    CloseParen,
    Comma,
    Period,
    OpenAngle,
    CloseAngle,
    OpenSquare,
    CloseSquare,
    Equal,
    LessGreater,
    BangEqual, 
    LessEqual,
    GreaterEqual,
    Plus,
    Minus,
    Asterisk,
    Slash,
    Percent,
    BarBar,
    DoubleArrow,

    // literals
    String,
    UnicodeString,
    BinaryLiteral,
    Integer,
    Decimal,
    Double,
    QuotedIdentifier,
    BackquotedIdentifier,

    // reserved words
    ALTER,
    AND,
    AS,
    BETWEEN,
    BIGINT,
    BOOLEAN,
    BY,
    CASE,
    CAST,
    CONSTRAINT,
    CREATE,
    CROSS,
    CUBE,
    CURRENT_DATE,
    CURRENT_PATH,
    CURRENT_TIME,
    CURRENT_TIMESTAMP,
    CURRENT_USER,
    DEALLOCATE,
    DECIMAL,
    DELETE,
    DESCRIBE,
    DISTINCT,
    DOUBLE,
    DROP,
    ELSE,
    END,
    ESCAPE,
    EXCEPT,
    EXECUTE,
    EXISTS,
    EXTRACT,
    FALSE,
    FOR,
    FROM,
    FULL,
    FUNCTION,
    GROUP,
    GROUPING,
    HAVING,
    IN,
    INNER,
    INSERT,
    INTEGER,
    INTERSECT,
    INTO,
    IS,
    JOIN,
    LEFT,
    LIKE,
    LOCALTIME,
    LOCALTIMESTAMP,
    NATURAL,
    NORMALIZE,
    NOT,
    NULL,
    ON,
    OR,
    ORDER,
    OUTER,
    PREPARE,
    REAL,
    RECURSIVE,
    RIGHT,
    ROLLUP,
    SELECT,
    SMALLINT,
    TABLE,
    THEN,
    TINYINT,
    TRUE,
    UESCAPE,
    UNION,
    UNNEST,
    USING,
    VALUES,
    VARBINARY,
    VARCHAR,
    WHEN,
    WHERE,
    WITH,
}
